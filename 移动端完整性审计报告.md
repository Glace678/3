# 移动端完整性审计报告（Android / iOS / HarmonyOS）

> 审计日期：2026-09-21 ｜ 审计方式：全量文件树核对 + 逐文件源码审读（静态）。
> 本环境无 NDK/Xcode/DevEco/Gradle 工具链，所有修复为源码级，未经真机构建验证；
> 文末附验证命令，请在具备工具链的机器上执行。

## 一、完整性矩阵（审计结论）

| 组件 | Android | HarmonyOS | iOS |
|---|---|---|---|
| 引擎平台入口 | ✅ `MuMain/src/source/App/Platform/Android/main.cpp` | ✅ `Platform/HarmonyOS/main.cpp` | ✅ **本次新增** `Platform/iOS/main.mm` |
| 引擎 CMake 接线 | ✅ `MU_MOBILE` 分支（共享 SDL3 / gl4es→GLES / libmain.so / 16KB 页对齐 / curl 注入） | ✅ 同左（`OHOS` 归一化 + `__OHOS__`） | 🔶 **本次新增** `MU_IOS_PREVIEW` 可选分支（默认仍明确报"暂不支持"） |
| 壳工程 | ✅ `OpenMU-Android/`（Gradle：game-app + gm-app，wrapper 齐全） | ✅ `OpenMU-HarmonyOS/`（hvigor ×3：game / gm / pc） | ✅ **本次新增** `OpenMU-iOS/`（CMake→Xcode 生成器 + Info.plist + 构建脚本） |
| .NET 协议库脚本 | ✅ `nativeaot/`（linux-bionic-arm64，soname 链接参数） | ✅ `nativeaot-ohos/`（linux-ohos-arm64，bionic 回退指引） | ✅ **本次新增** `nativeaot-ios/`（ios-arm64 实验路线 + @rpath install_name + 失败处置指引） |
| 触控/手势 | ✅ `Core/Input/MobileGestureMapper`（三端共享，单测已注册进 `core_input_timing_tests`） | ✅ 同左 | ✅ 同左（入口已关闭 SDL 触控合成鼠标） |
| 免登录/身份 | ✅ HMAC-SHA256 包密钥派生账号（`MobileIdentity`），私网地址白名单 | ✅ ArkTS `MobileIdentity/MobileConfig` 同源 | 🔶 入口环境变量约定已就位，壳层解包/注入待接 |
| GM 端 | ✅ 原生表单 App（`/api/mobile-gm`，服务端 API+鉴权+测试齐全） | ✅ 原生 ArkTS App | ✅ 走响应式管理面板（Safari/PWA，API 同源）——已在文档明确 |
| 构建/交付脚本 | ✅ `Build-AndroidPackage.ps1` 等 4 个 | ✅ hvigor 导入即用（文档诚实声明未产 .hap） | ✅ `build-ios.sh`（未真机验证） |
| 文档 | ⚠️ 过期（已修复） | ✅ `BUILD-鸿蒙构建说明.md` 完整 | ✅ **本次新增** README + 构建说明 |
| 密钥卫生 | ✅ .gitignore 全面（配对密钥/素材/预编译库均不入库），缺参即构建失败 | ⚠️ 占位密钥入库（已修复：构建期校验拦截） | ✅ 无密钥入库 |

**总体结论**：Android 与 HarmonyOS 为"源码完整、待真机验证"；iOS 原为"明确缺失"
（引擎 CMake 里是 FATAL_ERROR 占位），本次补齐为与鸿蒙同级的"脚手架完整、待移植验证"。

## 二、发现的问题与本次修复（5 项）

### 1.（Android·阻断级）NDK 路径硬编码到单台开发机
- 位置：`OpenMU-Android/game-app/build.gradle`（`ndkPath 'C:/Program Files (x86)/...'`）、
  `Build-AndroidPackage.ps1`（`$androidNdkRoot` 同路径 + 仅 Windows 的 llvm-strip 路径）。
- 影响：换任何一台机器（包括 macOS/Linux 的 CI）configure 即失败。
- 修复：gradle 改为 `-POPENMU_ANDROID_NDK_PATH` → `ANDROID_NDK_HOME` → `ANDROID_NDK_ROOT`
  → 缺省交给 AGP 按 `ndkVersion` 在 SDK 内解析；ps1 增加 `-NdkPath` 参数与同序环境
  变量回退，找不到时明确报错；llvm-strip 按宿主平台选择 `windows-x86_64/darwin-x86_64/linux-x86_64` prebuilt 目录。

### 2.（HarmonyOS·安全级）占位配对密钥与开发者内网地址入库
- 位置：`harmony-game|harmony-gm|harmony-pc` 三个 `entry/build-profile.json5`
  （`MOBILE_PACKAGE_KEY` = 43 个 A，即实现报告中公开的测试密钥；`DEFAULT_SERVER_ADDRESS` = 192.168.215.56）。
- 影响：安卓侧"缺真实密钥即构建失败"，鸿蒙侧却会**静默出包**——任何读过仓库的
  人都能用该弱密钥派生游戏账号并调用 GM API。
- 修复：三个工程的 `entry/hvigorfile.ts` 增加构建前校验：占位密钥/非私网地址直接
  拒绝构建，附替换指引；仅本地联调可设 `OPENMU_ALLOW_PLACEHOLDER_MOBILE_KEY=1` 豁免。
  （密钥值本身保留为占位符——真实密钥属机密，不应入库，出包时由构建者替换。）

### 3.（iOS·缺失级）iOS 版本完全缺失
- 现状：引擎 CMake 为显式 FATAL_ERROR 占位（注释预留了接入点），无入口、无工程、无脚本。
- 修复（本次新增，与安卓/鸿蒙同构）：
  - `MuMain/src/source/App/Platform/iOS/main.mm`：SDL3 iOS 引导（SDL_main 宏链路）、
    沙盒数据根（`MU_IOS_DATA_ROOT` → 回退 `Documents/Data`）、`MU_CONFIG_FILE` 注入、
    关闭触控合成鼠标（手势系统直接消费 finger 事件）。
  - `MuMain/src/CMakeLists.txt`：新增 `MU_IOS_PREVIEW` 选项；iOS 分支默认仍明确失败
    （防止把未完成目标误当可用），显式开启后选择 iOS 入口并警告移植状态。
  - `OpenMU-iOS/`：`game-ios/CMakeLists.txt`（壳工程，bundle/签名/资源约定）+
    `Info.plist.in`（横屏、arm64、iOS 15+、免加密声明）+ `build-ios.sh`（Xcode 生成器，
    真机/模拟器、DEVELOPMENT_TEAM 透传、-allowProvisioningUpdates）+
    `nativeaot-ios/`（协议库脚本 + install_name=@rpath props）+ README/构建说明 + .gitignore。
- 未竟事项（已在 `BUILD-iOS构建说明.md` 按依赖排序）：渲染层 gl4es→GLES2/Metal 真机
  验证、协议库 Frameworks 嵌入与 @rpath 加载、首启动数据解包、系统 IME、SafeArea、
  ProMotion、签名分发。**iOS 目前是脚手架，不是可玩版本**——这是诚实标注。

### 4.（文档·一致性）引擎构建文档与代码脱节
- `MuMain/docs/build/android/README.md` 仍写"没有 Android 入口、不受支持"（实际入口、
  CMake 分支、Gradle 工程、手势测试均已存在）→ 重写为真实现状 + 构建指引 + 状态表。
- `MuMain/docs/build/iOS/README.md` → 更新为脚手架现状 + 缺口清单。
- `MuMain/docs/build/README.md` "Planned" 段 → 改为 "Mobile" 段（三端现状各一句）。

### 5.（核对通过，无需修复的项）
- 手势/输入单测 `MobileGestureMapperTests.cpp` 已注册进 `tests/core/CMakeLists.txt` ✅
- 服务端移动 GM API（`MobileGm*` 8 文件 + 鉴权 3 文件 + 2 组测试）齐全 ✅
- 鸿蒙 `native_bridge.cpp`（NAPI 环境注入）与 CMake 预编译库 fail-fast 检查逻辑正确 ✅
- 鸿蒙 PC 版 abiFilters 含 arm64+x86_64，与文档一致 ✅
- 安卓/鸿蒙 .gitignore 对素材、密钥、预编译库、签名材料的排除完备 ✅

## 三、建议的验证步骤（有工具链的机器上）

```bash
# Android（Windows 开发机）
cd OpenMU-Android
$env:OPENMU_MOBILE_PACKAGE_KEY='<真实43位密钥>'
.\Build-AndroidPackage.ps1 -ServerAddress <内网IP> -NdkPath <NDK根目录>
.\Test-MobileLogic.ps1

# HarmonyOS（装好 DevEco 的机器）
# 直接构建应被新校验拦截；豁免验证：
OPENMU_ALLOW_PLACEHOLDER_MOBILE_KEY=1 hvigorw assembleHap   # 或在 DevEco 中构建
# 替换真实密钥后再正常构建

# iOS（macOS + Xcode 16）
cd OpenMU-iOS
./nativeaot-ios/build-clientlibrary-ios.sh
DEVELOPMENT_TEAM=<TeamID> ./build-ios.sh
```

## 四、本次变更文件清单

| 文件 | 类型 |
|---|---|
| `OpenMU-Android/game-app/build.gradle` | 修改（NDK 解析参数化） |
| `OpenMU-Android/Build-AndroidPackage.ps1` | 修改（-NdkPath/环境变量 + 跨宿主 llvm-strip） |
| `OpenMU-HarmonyOS/harmony-game/entry/hvigorfile.ts` | 修改（配对密钥构建校验） |
| `OpenMU-HarmonyOS/harmony-gm/entry/hvigorfile.ts` | 修改（同上） |
| `OpenMU-HarmonyOS/harmony-pc/entry/hvigorfile.ts` | 修改（同上） |
| `MuMain/src/source/App/Platform/iOS/main.mm` | **新增**（iOS 引擎入口） |
| `MuMain/src/CMakeLists.txt` | 修改（MU_IOS_PREVIEW + iOS 分支） |
| `OpenMU-iOS/`（8 个文件） | **新增**（完整 iOS 脚手架） |
| `MuMain/docs/build/android/README.md` | 重写（现状对齐） |
| `MuMain/docs/build/iOS/README.md` | 重写（脚手架状态） |
| `MuMain/docs/build/README.md` | 修改（Planned→Mobile） |
| `移动端完整性审计报告.md` | **新增**（本文件） |
